Input can be changed by modifying the contents of src/accounts.json

Change the directory to CrystalYeeAccountMerging

Run the command:
npm install

Run the command:
npx ts-node .\src\mergeAccounts.ts